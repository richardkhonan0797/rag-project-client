import React from 'react'

function ProjectsPage() {
  return (
    <div>
      This page shows all the projects that the user has created. It will fetch the projects from the database and display them in a list. Each project will have a link to view the project details and a button to delete the project. The user can also create a new project by clicking on the "Create Project" button.
    </div>
  )
}

export default ProjectsPage
